# airflow_tutorial

## Install

```
pip install -U pip setuptools wheel
pip install Cython
pip install pytz
pip install pyOpenSSL
pip install ndg-httpsclient
pip install pyasn1
pip install airflow
```
